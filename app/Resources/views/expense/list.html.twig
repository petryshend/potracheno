{% extends 'base.html.twig' %}

{% block body %}
    <table class="table">
        <thead>
            <tr>
                <th>Amount</th>
                <th>Created At</th>
            </tr>
        </thead>
        <tbody>
            {% for expense in expenses %}
                <tr>
                    <td>{{ expense.amount }}</td>
                    <td>{{ expense.createdAt|date }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}